#include <stdio.h>
int main(){
    for (int i=0;i<50;i++){
        int num;
        printf("Informe um número: ");
        scanf("%d", &num);
        if (num%2==0){
            printf("%d", num);
        }else{
            continue
        }
    }
    return 0;
}
